import { StyleSheet } from "react-native";


export const globalStyles = StyleSheet.create({

    pesosBacground: {
        width: "100%",
        height: 200,
        alignItems: "center",
        paddingTop: 16,
        marginBottom: 15,
    }
})