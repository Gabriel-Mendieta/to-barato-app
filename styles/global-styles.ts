import { StyleSheet } from "react-native";


export const globalStyles = StyleSheet.create({

    pesosBacground: {
        width: "100%",
        height: 250,
        alignItems: "center",
        paddingTop: 40,
        marginBottom: 15,
    }
})